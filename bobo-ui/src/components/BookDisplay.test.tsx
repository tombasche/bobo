import { genresToString } from './GenreDisplay';

test('Clicking on a card shows the detail view', () => {
    // create a card
    // fire a click event
    // check that the state has changed?

    expect(true).toBe(false);
});